name: Build Android APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'

      - name: Install NPM Dependencies
        run: npm install

      - name: Prepare Web Build
        run: |
          mkdir -p www
          cp index.html www/index.html

      - name: Create Android Project & Sync
        run: |
          npx cap add android || echo "Android folder already exists"
          npx cap sync android

      - name: Inject Bluetooth & Anti-Crash Permissions
        run: |
          # 1. Inject Izin Bluetooth Lengkap (Termasuk ADVERTISE untuk Android 12+)
          sed -i '/<application/i \
          <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" /> \
          <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" /> \
          <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" /> \
          <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" /> \
          <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" /> \
          <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> \
          <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> \
          <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />' android/app/src/main/AndroidManifest.xml
          
          # 2. Inject CleartextTraffic agar WebView tidak blokir data biner (Penting!)
          sed -i 's/<application/<application android:usesCleartextTraffic="true"/' android/app/src/main/AndroidManifest.xml

      - name: Grant Execute Permission
        run: chmod +x android/gradlew

      - name: Build APK Debug
        run: |
          cd android
          ./gradlew assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SukaLaundry-Printer-Pro
          path: android/app/build/outputs/apk/debug/app-debug.apk
