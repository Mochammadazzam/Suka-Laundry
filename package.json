{
  "name": "eppos-bt-tester",
  "version": "1.0.0",
  "scripts": {
    "build-web": "mkdir -p www && cp index.html www/index.html",
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor-community/bluetooth-le": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
